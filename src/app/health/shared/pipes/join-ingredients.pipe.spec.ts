import { JoinIngredientsPipe } from './join-ingredients.pipe';

describe('JoinIngredientsPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinIngredientsPipe();
    expect(pipe).toBeTruthy();
  });
});
